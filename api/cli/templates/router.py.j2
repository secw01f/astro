import logging

from fastapi import APIRouter, Request, Depends

from src.db.db import session_dep
{% if protected %}
from lib.auth.auth import verify_token

{{ name }}_router = APIRouter(prefix="/{{ name }}", dependencies=[Depends(verify_token)])
{% else %}
{{ name }}_router = APIRouter(prefix="/{{ name }}")
{% endif %}
logger = logging.getLogger(__name__)

# @{{ name }}_router.get("/")
# @{{ name }}_router.post("/")
